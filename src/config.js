// 配置项库 用于存放整个项目的url
const host = 'http://localhost:3000' // 本地开发域名
// const host = 'https://ohhucxts.qcloud.la' // 线上开发域名
const config = {
  host,
  loginUtl: `${host}/weapp/login`
}

export default config
